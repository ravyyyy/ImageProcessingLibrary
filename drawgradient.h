/**
   @file
 * @brief Generates a gradient pattern based on hue and intensity.
 * 
 * @param mira Pointer to the array where the gradient pattern will be stored.
 * @param windowWidth Width of the image canvas.
 * @param windowHeight Height of the image canvas.
 * @param columnsNumber Number of columns (colors) in the gradient pattern.
 */

void DrawGradient(unsigned char* mira, int windowWidth, int windowHeight, int columnsNumber);